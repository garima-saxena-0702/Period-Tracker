import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    calendar: {
        fontSize: 12,
        color: 'black',
        position: 'absolute',
        bottom: 0,
        left: 0,
        width: '100%',
        backgroundColor: 'beige'
    },
});
